# ── Dev용 Dockerfile (Next.js 핫 리로드) ──────
FROM node:20-alpine

RUN apk add --no-cache libc6-compat

WORKDIR /app

# 패키지 먼저 설치 (캐시 활용)
COPY package.json package-lock.json* ./
RUN npm ci

# 소스는 볼륨으로 마운트됨
EXPOSE 3000

ENV PORT=3000
ENV HOSTNAME="0.0.0.0"

CMD ["npm", "run", "dev"]
